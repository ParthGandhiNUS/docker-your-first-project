************************************************************************
*** This document contains the URLs and commands used in this course ***
************************************************************************

*** 01_02 ***
https://www.docker.com/products/docker-desktop/

https://code.visualstudio.com/download

*** 01_03 ***
https://github.com/LinkedInLearning/docker-your-first-project-4485003

https://git-scm.com/downloads

cd c:\projects\docker-your-first-project-4485003

git clone https://github.com/LinkedInLearning/docker-your-first-project-4485003.git

git checkout CHAPTER#_MOVIE#

git branch

*** 02_01 ***
Code for this video is in the dockerfile

*** 02_02 ***
docker build [OPTIONS] PATH | URL | -

https://docs.docker.com/engine/reference/commandline/build/

docker build .
<<<<<<< HEAD

docker build -f [dockerfile] .

docker build --force-rm=true .

docker build --rm=true .

docker build --no-cache .

docker build --help

*** 03_01 ***
docker search python
--> Mainly to search for images

docker search --filter is-official=true python
--> Filter out the unofficial images

docker search --filter stars=100 python
--> To get the most popular images

docker search --filter is-automated=true python
--> Tells us if we can build the images automatically from platforms such as github

docker search --filter is-official=true --filter stars=10 --filter is-automated=false python
--> Need to specify the filter argument for each individual filter

docker search --limit 4 python
--> To get the top 4 images

docker search --no-trunc python
--> "no-trunc" options Returns the full output of the search results w/o truncating anything -> good to get full image descriptions

#Other formatting options below
docker search --format "{{.Name}}: {{.Description}}" --no-trunc python

docker search --format "table {{.Name}}\t{{.IsAutomated}}\t{{.IsOfficial}}\t{{.Description}}\t{{.StarCount}}" python

docker search --format "table {{.Name}}\t{{.IsOfficial}}\t{{.Description}}\t{{.StarCount}}" --filter is-official=true --filter stars=10 --no-trunc python

docker search --help

docker image pull python

https://hub.docker.com/_/python

docker image pull python:3.12-rc-bookworm

docker image pull --help
=======
--> this builds the image using the root path of ou project

docker build -f [dockerfile] .
--> Good option to use if we have a different docker file for each environment, like QA, staging, or production
--> Enables us to test our application using any configuration, which is helpful for troubleshooting any errors 
which occur in specific environments

docker build --force-rm=true .
--> Removes an intermediate container which is made 
--> When we create a new image or temporary container for each instruction in our docker file such as run, copy, etc
--> Once the command is run in the intermediate container, that container is saved as a new image layer
--> the force rm option removes any intermediate containers that exist, even if the build is unsuccessful
  
docker build --rm=true .
--> You can use the "rm=true" option in case of an unsuccessful build, those intermediate containers will not be removed
--> Allows us to debug the last intermediate container or committing it as an intermediate image.

docker build --no-cache .
--> All previously build image layers are cached and can be reused, but if your installation depends on external resources, 
the docker cache can cause issues --> thats where the no-cache option comes in
--> Can use this option to tell docker to skip the cache and rebuild the entire image, 
--> May take a long time depending on the complexity of the project 

docker build --help
--> To view more options
>>>>>>> 02_02
